spring:
  application:
    name: edupulse_backend

  datasource:
    url: jdbc:postgresql://localhost:5432/edupulse_db
    username: postgres
    password: 1234567890

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

